import { DeleteDirective } from './delete.directive';

describe('DeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteDirective();
    expect(directive).toBeTruthy();
  });
});
